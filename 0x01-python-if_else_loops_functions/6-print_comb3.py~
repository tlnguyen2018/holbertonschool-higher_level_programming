#!/usr/bin/python3
for first in range(10):
    for second in range(10):
        if second > first:
            print(first, end='')
            if first != 8 and second != 9:
                print(second, end=', ')
            else:
                print(second)
