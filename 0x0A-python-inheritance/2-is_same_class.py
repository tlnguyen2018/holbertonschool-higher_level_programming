#!/usr/bin/python3
"""
Return True if obj same type with a_class
False for fail
"""


def is_same_class(obj, a_class):
    if type(obj) is a_class:
        return (True)
    else:
        return (False)
